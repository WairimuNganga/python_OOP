#A function can be defined with a default argument which is used if 
#no argument is given during invocation.
def my_country(country="Uganda",student="Susan"):
    return f"Hello,{student} are from,{country}"
